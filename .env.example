# This machine already has a system-wide PostgreSQL 15 server on port 5432
# (installed outside Homebrew), so the Homebrew postgresql@16 instance used
# for local dev was configured to listen on 5433 instead, to avoid touching
# that existing server. Adjust if your setup differs.
DATABASE_URL=postgresql://localhost:5433/healthquest
PORT=4000
